<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      type="text/css"
    />
  </head>
  <body>
    <div class="container">
      <ul>

        <h2 class="mt-4">Form</h2>
      </ul>
      <br>
      <form method="POST" action="/add">
        <div class="mb-3 row">
          <label for="string" class="col-sm-2 col-form-label">String</label>
          <div class="col-sm-10">
            <input
              type="text"
              id="string"
              class="form-control"
              placeholder="String"
              name="string"
              value="<%= data.string %>"
            />
          </div>
        </div>
        <div class="mb-3 row">
          <label for="integer" class="col-sm-2 col-form-label">Integer</label>
          <div class="col-sm-10">
            <input
              type="number"
              id="integer"
              class="form-control"
              placeholder="Integer"
              name="integer"
              value="<%= data.integer %>"
            />
          </div>
        </div>
        <div class="mb-3 row">
          <label for="float" class="col-sm-2 col-form-label">Float</label>
          <div class="col-sm-10">
            <input
              type="number"
              step="0.01"
              id="float"
              class="form-control"
              placeholder="Float"
              name="float"
              value="<%= data.float %>"
            />
          </div>
        </div>
        <div class="mb-3 row">
          <label for="date" class="col-sm-2 col-form-label">Date</label>
          <div class="col-sm-10">
            <input
              type="date"
              id="date"
              class="form-control"
              name="date"
              value= "<%= moment(data.date).format('YYYY-MM-DD') %>"
            />
          </div>
        </div>
        <div class="mb-3 row">
          <label for="boolean" class="col-sm-2 col-form-label">Boolean</label>
          <div class="col-sm-10">
            <select class="form-select" id="boolean" name="boolean">
              <option value="" disabled selected>Boolean</option>
              <option value="true" <%= data.boolean == true ? 'selected' : '' %>>True</option>
              <option value="false" <%= data.boolean == false ? 'selected' : '' %>>False</option>
            </select>
          </div>
        </div>
        <button type="submit" class="btn btn-primary btn-sm">Tambah</button>
        <a href="/data" class="btn btn-warning btn-sm">Kembali</a>
      </form>
    </div>
  </body>
</html>
